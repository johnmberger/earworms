/** sessionStorage flag — first visit splash already shown this tab */
export const SPLASH_KEY = "earworms-splash-seen";

/** Set on <html> by an inline script before paint when splash should cover the page */
export const SPLASH_PENDING_CLASS = "splash-pending";
